/*CREATE TABLE pets (
    id INT AUTO_INCREMENT PRIMARY KEY, -- Auto-generated primary key
    name VARCHAR(255) NOT NULL,        -- Name of the pet
    type VARCHAR(255) NOT NULL,        -- Type of pet (e.g., Dog, Cat)
    owner_name VARCHAR(255) NOT NULL,  -- Name of the pet's owner
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP -- Timestamp when the record is created
);
*/